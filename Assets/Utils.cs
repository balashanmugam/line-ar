using UnityEditor;
using UnityEngine;

namespace LineAR {
    public static class Utils {
        
        // Bala Bresnham's circle drawing algorithm 
        public static Vector3[] CreateCirclePoints() {
            // Step 1
            // Set initial conditions
            Vector3 point;
            float _d = 0;
            float _radius = 1;
            point.x = 0;
            point.y = _radius;
            _d = 3 - (2 * _radius);

            // step 2
            

            return null;
        }
        
        
    }
}